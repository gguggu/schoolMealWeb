import React from 'react';

const SchoolList = ({ schoolList }) => {
    console.log(schoolList);
    return (
        <div>
            {schoolList}
        </div>
    );
};

export default SchoolList;